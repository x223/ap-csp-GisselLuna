var X= 1;
var Y= 1;
var xpos =1;
var ypos = 1;
function setup() {
  createCanvas(500,500);
}

function draw() {
  background('skyblue');
  ellipse(60,60,60,60);

  
  
if (xpos > windowWidth) {
  xdir=xdir * -1;
  
} else { 
  
  
}
}